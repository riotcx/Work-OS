{
  "name": "work-os",
  "private": true,
  "version": "0.1.0",
  "description": "A local-first personal operating system for managing projects, weekly sprints, goals and execution.",
  "scripts": {
    "dev": "concurrently -n SERVER,CLIENT -c green,cyan \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "npm run dev --prefix server",
    "prod": "npm run build && npm run dev --prefix server",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
